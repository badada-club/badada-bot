{
    "name": "badada-bot",
    "version": "0.1.0",
    "type": "module",
    "description": "An app responsible for communitating with the 'Badada' Telegram bot.",
    "main": "dist/index.js",
    "scripts": {
        "build": "npm run prisma:migrate:prod && npm run prisma:generate:dev && npm run copy-prisma && npx tsc",
        "build:github": "npm run prisma:generate:dev && npm run copy-prisma && npx tsc",
        "build:dev": "npm run prisma:migrate:dev && npm run copy-prisma && npx tsc",
        "build:dev:watch": "npm run prisma:migrate:dev && npm run copy-prisma && npx tsc --watch",
        "start": "node --experimental-specifier-resolution=node dist/index.js",
        "test": "npx jest --config=jest.config.json",
        "test:watch": "npx jest --config=jest.config.json --watch",
        "lint": "npx eslint . --ext .ts",
        "copy-prisma": "copyfiles -u 1 src/**/prisma/**/generated/*.* src/**/prisma/**/generated/**/*.* dist/",
        "prisma:generate:dev": "npx prisma generate --schema ./src/db/prisma/badada/badada.prisma",
        "prisma:migrate:dev": "npx prisma migrate dev --schema ./src/db/prisma/badada/badada.prisma",
        "prisma:migrate:prod": "npx prisma migrate deploy --schema ./src/db/prisma/badada/badada.prisma"
    },
    "keywords": [],
    "author": "Badada IT",
    "license": "ISC",
    "engines": {
        "node": "16.x"
    },
    "dependencies": {
        "@prisma/client": "^4.0.0",
        "axios": "^0.27.2",
        "cors": "^2.8.5",
        "dotenv": "^16.0.1",
        "express": "^4.18.1"
    },
    "devDependencies": {
        "@types/cors": "^2.8.12",
        "@types/express": "^4.17.13",
        "@types/jest": "^28.1.2",
        "@types/node": "^18.0.0",
        "@types/supertest": "^2.0.12",
        "@typescript-eslint/eslint-plugin": "^5.28.0",
        "@typescript-eslint/parser": "^5.28.0",
        "copyfiles": "^2.4.1",
        "eslint": "^8.17.0",
        "eslint-plugin-jest": "^26.5.3",
        "jest": "^28.1.1",
        "prisma": "^4.0.0",
        "supertest": "^6.2.3",
        "ts-jest": "^28.0.5",
        "typescript": "^4.7.3"
    }
}